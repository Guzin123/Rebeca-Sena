export interface ImageData {
  src: string;
  alt: string;
  category?: string;
}

export interface FeatureData {
  title: string;
  description: string;
  icon: string;
}

export interface StepData {
  number: string;
  title: string;
  description: string;
}